{
  "name": "reelpuzzels",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "validate": "esbuild scripts/validate.ts --bundle --platform=node --format=esm --outfile=scripts/.validate.mjs && node scripts/.validate.mjs",
    "smoke": "esbuild scripts/smoke.tsx --bundle --platform=node --format=esm --jsx=automatic --outfile=scripts/.smoke.mjs && node scripts/.smoke.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
